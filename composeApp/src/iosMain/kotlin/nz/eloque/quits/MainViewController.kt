package nz.eloque.quits

import androidx.compose.ui.window.ComposeUIViewController

/** Entry point consumed by the SwiftUI `iosApp` wrapper. */
fun MainViewController() = ComposeUIViewController { App() }
